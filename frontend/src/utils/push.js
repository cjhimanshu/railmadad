// Push notifications removed — no-op stubs keep imports from breaking
export async function registerPush() {}
export async function unregisterPush() {}
export function setupForegroundMessages() {
  return () => {};
}
